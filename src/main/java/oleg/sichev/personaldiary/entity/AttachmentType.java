package oleg.sichev.personaldiary.entity;

public enum AttachmentType {
    IMAGE, // Картинки (JPEG, PNG, GIF)
    DOCUMENT, // Документы (PDF, DOC, TXT)
    AUDIO, // Аудиофайлы (MP3, WAV, OGG)
    VIDEO // Видеофайлы (MP4, WebM)
}
